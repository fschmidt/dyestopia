# Pin to the public registry. Without this, the global ~/.npmrc points npm at the
# i19s Nexus mirror, which bakes nexus.i19s.com URLs into package-lock.json and
# makes `npm ci` fail with E401 on the GitHub Actions runner.
registry=https://registry.npmjs.org/
